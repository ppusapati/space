{
  "name": "@chetana/api-client",
  "version": "0.0.1",
  "description": "Chetana platform JSON API + WebSocket clients (IAM, audit, realtime). Hand-authored against the chetana cmd-layer JSON handlers + the realtime gateway sub-protocol; will be replaced by generated Connect clients once iam.proto / audit.proto / realtime.proto regenerate (gated by OQ-004).",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./iam": "./src/iam.ts",
    "./audit": "./src/audit.ts",
    "./realtime": "./src/realtime.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
