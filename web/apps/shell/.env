# Local dev — points the SvelteKit shell at the running monolith on :9090.
# Created 2026-04-29 for the live walkthrough screen demo.
# Backend: see backend/app/cmd/main.go (PORT=9090 in the launch command).
VITE_API_URL=http://localhost:9090
